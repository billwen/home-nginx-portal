name: Build Image

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:

  generate-jobs:
    name: Generate Jobs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3